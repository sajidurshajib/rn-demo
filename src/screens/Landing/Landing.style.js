import { StyleSheet } from 'react-native'
import { colors } from '../../styles'

const styles = StyleSheet.create({
    container: {
        backgroundColor: '#fafafa',
        alignItems: 'center',
        width: '100%',
        padding: 10,
        marginTop: 5,
    },
    greet: { color: colors.LIGHT_DARK },
})

export { styles }
